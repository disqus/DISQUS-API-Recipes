<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DisqusSDKDemoAndroidApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.DisqusSDKDemoAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ArticleDetailActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <!-- App Link: staging — OS intercepts mobileauth/success before Chrome loads the page -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="casualgamer.saas-dev.disqus.org"
                    android:pathPrefix="/mobileauth/success" />
            </intent-filter>
            <!-- App Link: production -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="casualgamer.disqus.com"
                    android:pathPrefix="/mobileauth/success" />
            </intent-filter>
            <!-- Custom scheme fallback for app-level login (DisqusAuthManager.startLogin) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="disqussdkdemoandroid"
                    android:host="mobileauth"
                    android:pathPrefix="/success" />
            </intent-filter>
        </activity>

    </application>

</manifest>
